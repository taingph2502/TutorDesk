# Copy this file to .env and replace the value before starting TutorDesk.
# Compose mounts it as a container secret; it is not placed in a service environment variable.
TUTORDESK_DATABASE_PASSWORD=replace-with-a-long-random-local-password
