# Cross-package tests

Repository-level integration tests exercise public interfaces across package boundaries. Unit tests
remain beside their owning package. Tests must not inspect another module's tables or storage paths.
